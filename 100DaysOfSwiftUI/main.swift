//
//  main.swift
//  100DaysOfSwiftUI
//
//  Created by Paulo Henrique on 22/05/24.
//

/*
 cmd + K = Limpa cache gerado na compilação
 cmd + B = Compila projeto
 cmd + R = Compila e Executa projeto
 cmd + shift + Y = mostra / esconde parte de baixo
 cmd + 0 = mostra / esconde sidebar à esquerda
 cmd + opt + 0 = mostra / esconde sidebar à direita
 cmd + opt + T = mostra / esconde barra acima
 cmd + opt + { ou } = sobe ou desce linha atual
 cmd + a = seleciona tudo
 cmd + i = identa linha(s) selecionada(s)
 cmd + seta esquerda = vai pro começo da linha
 cmd + seta direita = vai pro fim da linha
 cmd + shift + seta esquerda ou direita = seleciona conteúdo da linha
 opt + shift + seta esquerda ou direita = seleciona texto até whitespace (palavra)
 cmd + shift + O = abre qualquer arquivo do projeto
 cmd + shift + L = abre a Library (o mais no topo a direita que tem views, modifiers, assets, colors, symbols)
 ctrl + barra de espaço = mostrar autocomplete
 ctrl + M antes do primeiro parametro de uma chamada de função = desce todos os parametros identados
 tab quando tem placeholders nos parametros = vai para o parametro
 tab quando tem placeholders nos parametros + enter = preenche o parametro. Se for uma closure, monta a trailing closures pra você.
 cmd + clica numa função, tipo ou variável = vai pra definição da função
 opt + clica numa função, tipo ou variável = mostra documentação
 */

import Foundation

Day7.main()


//func soma(a: Int, b: Int, c: () -> Void) -> Int {
//    return a + b
//}
